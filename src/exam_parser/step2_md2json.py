"""Step 2：Markdown 转换为 JSON（使用 LLM Structured Outputs）。"""

from pathlib import Path


def markdown_to_json(md_path: Path, output_dir: Path) -> Path:
    """调用 OpenRouter API 将 Markdown 解析为结构化 JSON，返回 JSON 文件路径。"""
    raise NotImplementedError("Step 2: markdown_to_json 尚未实现")
